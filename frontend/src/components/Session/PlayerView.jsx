const PlayerView = ({ campaign }) => {
  return (
    <div>
      <h2>Player View – {campaign.name}</h2>
      <p>Here you’ll explore, roll dice, and interact with the map.</p>
    </div>
  );
};

export default PlayerView;
